{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-capability",
  "description": "Automatic updates. Windows only, main window only.",
  "platforms": ["windows"],
  "windows": ["main"],
  "permissions": [
    "updater:allow-check",
    "updater:allow-download",
    "updater:allow-install"
  ]
}
