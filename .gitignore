# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Bundled FFmpeg sidecar - fetched by `npm run setup:ffmpeg`, never committed
src-tauri/binaries/*.exe
src-tauri/binaries/*.dll

# Updater signing keys - the private key lives outside this repo entirely.
# This is belt and braces in case one is ever generated in the working tree.
*.key
*.key.pub
*.password.txt
